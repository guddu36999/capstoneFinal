<!DOCTYPE html>
<html lang="en">
<head>
    <%-include('../partials/head')%>
    <title>Document</title>
</head>
<body class="p-4">
    <%-include('../partials/navbar')%>
    <h1 class="text-5xl md:text-[12rem] font-[700] m-6 text-center text-green-600">
        Home paGe\.
    </h1>
    <%if(locals.data){%>
        <%for(let i=0;i<data.length;i++){%>
            <div class="flex flex-col border-1 rounded-sm mt-10 mx-auto p-3 w-full">
                <h1 class="text-2xl font-bold"><%=data[i].title%></h1>
                <p>Timngs:<%=data[i].timings%></p>
                <p>Address:<%=data[i].address%></p>
                <p>Description:<%=data[i].description%></p>
                <p>By:<%=data[i].name%></p>
                <p>Contact:<%=data[i].contact%></p>
                <img src="<%=data[i].imageUrl%>" alt="" class="w-full">       
            </div>
         <%}%>
    <%}%>
    <%if(!locals.user){%>
        <p class="text-center text-blue-300">login Pleae to see the donations happening in your city!</p>
    <%}%>
</body>
</html>